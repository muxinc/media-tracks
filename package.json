{
  "name": "media-tracks",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "esbuild src/index.ts --bundle --outdir=dist --servedir=.",
    "build": "rm -rf dist && esbuild src/index.ts --target=es2019 --format=esm --bundle --minify --outdir=dist"
  },
  "author": "@muxinc",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.42",
    "eslint": "^8.17.0",
    "prettier": "^2.6.2",
    "typescript": "^4.7.3"
  },
  "prettier": {
    "tabWidth": 2,
    "singleQuote": true,
    "semi": true
  },
  "eslintConfig": {
    "env": {
      "browser": true,
      "es6": true,
      "node": true
    },
    "extends": [
      "eslint:recommended"
    ],
    "parserOptions": {
      "ecmaVersion": 2022,
      "sourceType": "module"
    }
  }
}
